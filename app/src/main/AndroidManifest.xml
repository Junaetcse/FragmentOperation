<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bangladesh.user.fragmentoperation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragmentPart1.FragmentAdditional" />
        <activity android:name=".activityToFragment.ParseDataAF" />
        <activity android:name=".fragmentToActivity.DataParseFA" />
        <activity android:name=".fragmentToFragment.DataParseFF"></activity>
    </application>

</manifest>